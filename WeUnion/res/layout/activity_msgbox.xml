<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    android:orientation="vertical"
      android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" 
    tools:context=".Msgbox" >

    <ListView
        android:id="@+id/msg"
        android:layout_width="fill_parent"
        android:layout_height="316dp"
        android:layout_alignParentTop="true"
        android:layout_weight="1.86"
        android:stackFromBottom="true" >

    </ListView>

    <EditText
        android:id="@+id/post_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/msg_event"
        android:layout_alignParentBottom="true"
        android:ems="10"
        android:inputType="textMultiLine" >

        
    </EditText>

    <Button
        android:id="@+id/post_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/post_msg"
        android:layout_alignBottom="@+id/post_msg"
        android:layout_toRightOf="@+id/post_msg"
        android:text="Post" />

</RelativeLayout>